# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# internal-only: the bot's source-of-truth brief. Contains the comp floor,
# honest weaknesses, rebuff pool phrasings, and other material that should
# not ship in a public repo. Deployed separately (see notes below).
PROFILE_BRIEF.md

# defensive: scratch/temp files that occasionally show up during local work
*.tmp
*.bak
*.swp
*.swo

# lighthouse / pagespeed audit artifacts
lighthouse-*.json
lighthouse-*.html
.lighthouseci/

# resumes, headshots, and any other personal artifacts dropped at the root
/Jared-Malcolm-*.pdf
/resume*.pdf
/*-headshot.*

# JD drop-zone for the /cv slash command. Track the directory itself
# (via .gitkeep) but never commit recruiter-supplied JD bodies.
/jd-input/*
!/jd-input/.gitkeep

# Tailored CVs generated by the /cv slash command. Each artifact is
# JD-specific and may contain recruiter-supplied details. Track the
# directory via .gitkeep, never the generated artifacts.
/cv-output/*
!/cv-output/.gitkeep

# Resume drafts (HTML, DOCX, PDF working files). Same drop-zone
# pattern as jd-input — track the directory via .gitkeep, never the
# generated artifacts.
/resume-drafts/*
!/resume-drafts/.gitkeep

# Personal draft of LinkedIn profile copy. Stays in the working tree
# as a reference for Jared but does not belong in repo history.
/LINKEDIN_UPDATES.md
RECRUITER_TRACKING.md
